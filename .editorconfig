[*]
charset = utf-8
insert_final_newline = true

# The settings for C (*.c and *.h) files are mirrored in .clang-format.  Keep
# them in sync.
[*.{cpp,c,h,sh,perl,pl,pm,txt}]
indent_style = tab
tab_width = 4